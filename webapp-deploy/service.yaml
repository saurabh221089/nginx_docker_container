apiVersion: v1
kind: Service
metadata:
  name: nginx-service  
  annotations:
    metallb.io/loadBalancerIPs: 192.168.1.31
spec:
  selector:
    app: nginx
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    #nodePort: 30081
